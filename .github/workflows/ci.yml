on:
  pull_request:
    branches: [main]
    types: [opened, reopened, synchronize, edited]

jobs:
  ci:
    name: vet and build source
    runs-on: ubuntu-latest

    steps:
      - name: Checkout
        uses: actions/checkout@v3

      - name: Run pre-commit hooks
        run: make precommit
